package com.epam.hotel.web.command;

public enum CommandName {
    LOGIN, LOGOUT, REGISTER, ROOM_LIST, SEARCH_RESULT, INDEX_PAGE, REGISTER_PAGE, CHANGE_LOCALE,
    LOGIN_PAGE, ROOM_INFO, PROFILE, BOOK;
}
